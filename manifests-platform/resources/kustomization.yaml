# =============================================================================
# Platform Resources - Root Kustomization
# =============================================================================
# Includes all platform resources managed by ArgoCD.
# Add or remove subdirectories here to control what gets deployed.
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cert-manager/
  - external-dns/
  - external-secrets/
  - fortiweb-bootstrap/
  - fortiweb-controller/
