# =============================================================================
# TLS Certificates for FortiWebIngress Gateway
# =============================================================================
# Per-app TLS certificates managed by cert-manager. The FortiWebIngress
# controller reads these secrets and uploads certificates to FortiWeb.
#
# Certificate secrets are created in fortiweb-controller namespace for
# easy access by the controller.
# =============================================================================
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: canada-tls
  namespace: fortiweb-controller
spec:
  secretName: canada-tls
  privateKey:
    rotationPolicy: Always
  commonName: canada.amerintlxperts.com
  dnsNames:
    - canada.amerintlxperts.com
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: latam-tls
  namespace: fortiweb-controller
spec:
  secretName: latam-tls
  privateKey:
    rotationPolicy: Always
  commonName: latam.amerintlxperts.com
  dnsNames:
    - latam.amerintlxperts.com
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: docs-tls
  namespace: fortiweb-controller
spec:
  secretName: docs-tls
  privateKey:
    rotationPolicy: Always
  commonName: docs.amerintlxperts.com
  dnsNames:
    - docs.amerintlxperts.com
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
